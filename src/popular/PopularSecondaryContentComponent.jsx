import React from "react";

const PopularSecondaryContentComponent = ({ title, heading }) => {
  return (
    <div>
      <h5>{title}</h5>
      <h2>{heading}</h2>
    </div>
  );
};

export default PopularSecondaryContentComponent;
